// string stream
string data ="1,2,3,4,5,5,f,fgs, #, aa";
stringstream s(data) ; // or string s= data;
while(getline(s, str, ','){
// s is the string strem, str is the string that is extracted from s which have a sepreator ','

// str will be '1' then it will give '2' and so on.......


}